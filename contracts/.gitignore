# build output and caches
out/
cache/
# dependencies are restored with `forge soldeer install` from soldeer.lock; never commit them
dependencies/
lib/
# local/anvil runs and dry runs
broadcast/*/31337/
broadcast/**/dry-run/
deployments/31337.json
deployments/*.dry-run.json
.env
# echidna campaign output
crytic-export/
echidna-corpus/
